services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        - NODE_VERSION=20
    image: second-talent-fe-test:latest
    container_name: second-talent-fe-test
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
